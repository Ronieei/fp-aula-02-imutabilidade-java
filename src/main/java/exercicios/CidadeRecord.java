package exercicios;

// Classe que representa uma cidade, utilizando um record.
// O record é imutável por padrão. Portanto, o nome da cidade não pode ser alterado após sua criação.
// (immutable)
public record CidadeRecord(String nome) {
}
